# 版本对比：Textbook Creator Skill 初始版与当前版

## 对比范围与基线

本文基于 Git 历史中的真实提交，对比首个发布版本与当前版本：

| 版本 | 提交 | 说明 |
|---|---|---|
| 初始版 | `98c122d` | 首个独立发布的可扩展教材生产流程 |
| 中间版 | `71c379e` | 加入人类风格、编辑反馈和多主题渲染 |
| 当前版 | `cbf9868` | 加入风险分级审核和主题预览验证 |

对比聚焦于三个维度：

1. **内容契约**：教材必须包含什么，以及正文应该如何组织和表达。
2. **工作流程**：Agent 如何从用户请求推进到最终发布产物。
3. **监督方式**：作者、审核者、用户和发布门禁如何共同控制质量。

当前版本不是简单地把规则全部加严。它保留了初始版的正确性要求，同时删除或放宽那些不能降低实际风险的流程。

## 总体结论

| 领域 | 初始版 | 当前版 | 净变化 |
|---|---|---|---|
| 核心目的 | 具有线性学习路径的严谨大学教材 | 保持原目标，并增加长内容扩展、风格校准、风险审核和主题渲染 | 运行模型更完整 |
| 内容单元 | 以章节契约为核心，基本固定的十段式章节路径 | 保留章节契约，但明确它是覆盖指南，不是机械写作模板 | 更灵活，机械感更低 |
| 用户调研 | 简短总结和缺失问题 | 一个集中式决策表，包含路径、时长、目录、风格、审核节奏和主题 | 开始写作前确认更充分 |
| 学习路径 | 能力图、前置知识和大纲确认 | 保持原结构，并增加路径检查点和章节时间预算 | 用户确认更明确 |
| 长内容 | Markdown 优先的 Sub-agent 流程 | 增加选择性上下文任务包和主题阶段协议 | 上下文成本更低，分派更清晰 |
| 短内容 | 作为长内容流程的例外被提及 | 明确使用 `light` 审核并定义升级条件 | 流程选择更合理 |
| 人类风格 | 主要由清晰度和教学规则隐含保证 | 增加风格契约、参考样例、正向风格目标和用户编辑决定 | 明显增强 |
| Sub-agent 监督 | 有边界的角色、Markdown 产物和 `pass/revise/block` | 保留原机制，并增加失败恢复和输入隔离 | 操作可靠性更高 |
| 审核深度 | 默认描述为五轮审核 | 使用 `light`、`standard` 和 `high-stakes` 三档 | 减少过度处理，风险边界更清楚 |
| 人类审核 | 通过检查点建议，但不是发布状态 | 明确区分 `agent-reviewed`、`human-reviewed` 和 `verified` | 减少对自我审核的依赖 |
| 渲染 | 固定的离线 HTML/PDF 视觉系统 | 四种主题、命令行预览、manifest 主题字段和严格主题验证 | 场景支持更好 |
| 验证 | 结构、答案隔离、链接、公式和 PDF 检查 | 增加主题一致性和预览覆盖参数验证 | 工具契约更强 |

## 一、内容契约

### 1.1 两个版本共同保留的内容

初始版和当前版在基础目标上是一致的。两个版本都不把教材当作一篇很长的摘要。

两个版本都要求：

- 默认面向具备一定教育背景、进行独立学习的成年人；
- 在完整写作前先定义终端能力；
- 建立前置知识图和一条推荐的线性学习路径；
- 明确定义、假设、符号、机制、推导和适用边界；
- 提供展示中间推理的逐步工作例子；
- 从冻结后的题目记录生成练习；
- 独立生成答案册；
- 学生版 HTML 中物理删除答案内容；
- 为代码记录版本、输入/输出契约、测试、复杂度、失败模式和诚实的 `not executed` 标记；
- 在存在相应风险时进行证据、数学/科学、代码、教学和出版检查；
- 使用带有离线资源和静态回退的可移植 HTML/PDF 输出。

当前版没有削弱正确性和可复现性要求。它改变的是这些要求何时应用，以及如何应用。

### 1.2 初始版内容模型

初始版规定了稳定的章节路径：

1. 章节契约和诊断题；
2. 动机问题；
3. 直觉模型及类比的边界；
4. 正式术语和符号；
5. 推导、证明、算法、机制或实验；
6. 工作例子；
7. 非模仿练习；
8. 变化、反例、失败或边界；
9. 回到开头问题进行整合；
10. 总结、常见错误、练习和下一章桥接。

这个模型对内容覆盖和学习者进阶很强。它的风险是可能鼓励机械填槽。作者可以形式上完成十个标题，但正文仍然可能有完全相同的节奏、泛化的过渡句或解释过薄的问题。

### 1.3 当前版内容模型

当前版保留章节路径，同时增加三项控制：

- 章节路径是内容覆盖契约，不是 prose 模板。如果学习问题需要，可以调整章节顺序，但必须在大纲中解释原因。
- 风格契约记录语域、立场、密度、读者称呼方式、例子类型、个性程度、避免项和可选的用户样例。
- 使用三轮写作循环，分别处理推理、教学和表达风格。

当前版的风格目标是正向定义，而不是只做删除。它要求：

- 先从具体学习者问题开始，再提出抽象承诺；
- 使用明确的主语和动词，而不是由旁白宣布本节要做什么；
- 让段落节奏有变化；
- 在例子中展示决策、检查、不确定性和失败恢复；
- 使用表达因果、对比、结果或细化关系的过渡句；
- 准确表达不确定性，不制造虚假确定性；
- 为记忆和提取保留有目的的重复；
- 说明具体取舍，而不是给出无条件的普遍建议。

当前版还明确禁止：

- 虚构个人经验；
- 虚构课堂反应；
- 虚构从业者共识；
- 没有依据的工具、指标或因果关系。

### 1.4 内容分类

| 问题 | 初始版 | 当前版 |
|---|---|---|
| 如何识别长内容？ | 大约超过 60 分钟，有多个前置知识，同时包含理论和实现/案例，或跨章节复用 | 保持这些指标，并记录到 `content_scale`，同时关联审核配置 |
| 如何处理短内容？ | 允许直接写作，但至少有目标、例子、边界、自测和桥接 | 保持最低要求，并明确使用 `light` 审核以及升级规则 |
| 是否有中等内容类别？ | 没有 | 没有单独的中等类别；`standard` 是章节和模块的实际中间审核档位 |
| 什么时候必须升级短内容？ | 被复用或扩展时 | 被复用、需要推导/实验，或超过一个学习单元时 |
| 严谨程度是否由篇幅单独决定？ | 容易被 60 分钟阈值单独推断 | 明确不会：审核档位由风险决定，而非只由字数决定 |

保留这种简化是有意的。skill 使用“短内容/长内容”两种内容规模和三种审核档位。再增加一个中等内容规模，只会增加术语，不会改善任务分派决定。

## 二、工作流程对比

### 2.1 Intake 与用户互动

| 阶段 | 初始流程 | 当前流程 |
|---|---|---|
| 第一次回应 | 简短 intake 总结和缺失问题 | 简短 intake 总结加一张集中式决策表 |
| 必须确认的学习者信息 | 学习者、目标、深度、时长、语言、工具、资料和输出限制 | 包含初始信息，并增加单次/总学习时长、目录、写作方式、风格、人类审核节奏和渲染主题 |
| 用户确认 | 确认路径或接受记录的假设 | 确认 brief、路径、代表性样例和发布包 |
| 互动方式 | 按顺序逐个询问缺失信息 | 只询问会实质改变工作的决定，并集中在一次互动中提出 |
| 何时开始写作 | intake 和大纲确认后 | 长项目必须先确认或接受关键默认值，才开始完整内容生成 |
| 检查点频率 | 生产过程中提供章节检查点 | 四个有实际意义的检查点；除非用户要求，不在每个小节后打断 |

当前流程在正式写作前增加了互动，但在写作过程中减少了打断。这解决了两个看似冲突的要求：既要在开始前充分确认，又不能让用户批准每一个段落。

### 2.2 研究与学习设计

初始流程要求在正文前生成以下产物：

1. `brief.json`；
2. source register；
3. evidence map；
4. competency graph；
5. learning path；
6. objective matrix。

当前版保留全部六项。主要变化是上下文选择：

| 初始行为 | 当前行为 |
|---|---|
| 在相应阶段读取较广泛的研究和写作参考 | 使用阶段映射和最小参考包 |
| Sub-agent 提示词可能接收大量重复上下文 | 传递包含目标、前置知识、本地术语、源版本、输出契约和审核标准的紧凑任务包 |
| 审核指南可能全局加载 | 只加载当前审核档位和相关 rubric |
| 风格要求分散在一般写作规则中 | 只有在需要风格、样例或自然度时才加载风格参考 |

当前版通过减少重复参考文本来节省上下文，而不是通过省略作者做局部决定所需的事实来节省上下文。

### 2.3 主题生产顺序

初始的可扩展流程已经规定了主题顺序：

```text
大纲制定
  -> 内容构成判断
  -> 可视化/案例分析
  -> 具体 Markdown 制作
```

当前版保留这个顺序，并增加在规模化写作前进行风格/样例确认的阶段。长项目现在实际采用：

```text
brief 确认
  -> 路径和目录确认
  -> 主题协议
  -> 一个语气/视觉样例
  -> Markdown 草稿
  -> 风险分级审核
  -> 冻结练习
  -> 学生版/答案版编排
  -> 构建和发布审核
```

### 2.4 Sub-agent 工作流

| 职责 | 初始版 | 当前版 |
|---|---|---|
| 总体目录和学习路径 | 主 Agent | 主 Agent |
| 章节拆分 | 主 Agent | 主 Agent，并明确内容构成和视觉/案例判断 |
| 正文写作 | 有边界的内容 Sub-agent，输出 Markdown | 保持原方式，并增加风格契约和紧凑任务包 |
| 试题生成 | 内容冻结后由试题 Sub-agent 生成 | 保持原方式，并增加独立重算和代码契约检查 |
| 学生版 | 不接收答案字段的学生版编排 Agent | 保持原方式，并增加人类样例检查点和明确状态记录 |
| 答案册 | 独立答案编排 Agent | 保持原方式，并增加题目不可变和阻塞问题报告 |
| 审核 | 新鲜上下文、只读的审核 Sub-agent | 保持原方式，并增加风险档位和人类/Agent 意见分离 |
| 合并权 | 主 Agent | 仍然只能由主 Agent 合并，并进一步强化 |
| 失败产物 | 无效产物，不得合并 | 标记为 `blocked`，从最后一个有效 Markdown 检查点恢复，并记录失败 |
| 共享文件 | 避免并发写入 | 避免并发写入，并进一步隔离输入和输出集合 |

操作原则没有变化：Sub-agent 是有边界的贡献者，不能成为独立出版者。当前版明确了恢复行为，因为第一次教材生产已经证明：格式错误、缺失或无法恢复的结构化输出不能被误认为完成的章节包。

### 2.5 试题和答案流程

两个版本都要求先冻结题目，再生成答案。当前版增加了更明确的隔离：

- 学生版编排 Agent 不接收答案正文、评分点、答案专用 ID 或答案文件路径；
- 答案册编排 Agent 不能修改题目；
- 最终检查验证答案字段和答案文本在学生版中物理不存在；
- 发现答案问题时，必须作为阻塞问题报告，不能在答案生成阶段悄悄修改题目。

### 2.6 审核流程

初始审核流程：

```text
冻结源稿
  -> 证据审核
  -> 数学/科学审核
  -> 代码审核
  -> 教学审核
  -> 出版审核
  -> 修复阻塞问题并重新检查
```

当前审核流程：

```text
选择风险档位
  -> 只运行相关的机械检查和领域检查
  -> 将发现分类为 blocker、major 或 editorial
  -> 修复并重新检查 blocker
  -> 对 major 做修复、缩小范围或带后果的明确延期决定
  -> 将 editorial 意见交给人类审核者
  -> 完成人类所需检查点
  -> 设置 agent-reviewed、human-reviewed 或 verified 状态
```

当前流程对低风险内容更简单，同时在自动审核无法解决的关键问题上更严格：结果是否真正符合用户期望的教师语气和教学方式。

## 三、监督与治理方式

### 3.1 初始监督模型

初始版已经有较强的内部监督：

- 明确的角色边界；
- Markdown 中间产物；
- 新鲜上下文的审核 Sub-agent；
- `pass/revise/block` 结果；
- 带位置、证据、修复和复验信息的精确问题记录；
- 对事实、数学、代码、答案隔离和出版问题设置硬阻塞；
- 不接受只有摘要或格式错误的 Sub-agent 产物。

它的问题不是没有监督，而是监督大多发生在 Agent 与 Agent 之间，或来自 Agent 自我审核。用户反馈虽然被描述为检查点，但没有被表示成一个真正的发布状态。

### 3.2 当前监督模型

当前版有四层监督：

#### 第一层：主 Agent 治理

主 Agent 拥有：

- 终端能力；
- 依赖图；
- 章节顺序；
- 任务拆分；
- 稳定 ID；
- 合并和冲突解决；
- 发布决定。

这可以防止“分布式作者”进一步变成“分布式权力”。

#### 第二层：独立 Agent 审核

审核 Sub-agent 在新鲜、只读上下文中工作。它们输出基于证据的问题，而不是自我宣布的质量分数。审核结果可以是 `pass`、`revise` 或 `block`。

#### 第三层：风险分级的机械和领域门禁

当前档位如下：

| 档位 | 适用范围 | 要求的监督 |
|---|---|---|
| `light` | 短内容、低风险、无代码内容 | 目标、前置知识、事实、例子、边界和相关答案检查 |
| `standard` | 章节、模块、技术指南、教材 | 证据、相关数学/科学、代码、教学、出版、答案隔离和人类检查点 |
| `high-stakes` | 医疗、法律、金融、安全关键、受监管或高后果内容 | standard 检查，加独立领域专家或权威人类审核 |

#### 第四层：人类编辑权

用户是以下事项的最终决定者：

- 目标语气；
- 节奏；
- 文化适配；
- 例子选择；
- 感知上的自然度；
- 可接受的正式程度；
- 多个主题都可用时的渲染选择。

Agent 可以指出潜在问题，但不能通过给自己一个“通过分数”来关闭主观编辑意见。

### 3.3 状态治理

| 状态 | 初始版含义 | 当前版含义 |
|---|---|---|
| `draft` | 内容或检查尚未完成 | 内容或必需检查尚未完成 |
| `agent-reviewed` | 没有单独定义 | 自动检查和 Agent 检查完成，但用户尚未完成确认 |
| `human-reviewed` | 没有单独定义 | 所需人类检查点完成，并已记录决定 |
| `verified` | 只要没有阻塞问题就使用 | 风险档位通过、所需人类审核完成且没有阻塞问题 |

这个区分很重要，因为“Agent 检查过了”和“目标人类读者批准了路径和语气”是两种不同的事实。

### 3.4 问题监督

| 问题类型 | 初始处理 | 当前处理 |
|---|---|---|
| 事实/数学/代码/答案/出版错误 | 阻塞并重新检查 | 阻塞并重新检查 |
| 缺少桥接或严重不一致 | 审核发现，完成前修复 | 标记为 `major`，修复、缩小范围，或记录带后果的延期 |
| 语气/节奏/例子偏好 | 作为一般教学/风格问题审核 | 标记为 `editorial`，交给用户并保留用户决定 |
| 用户不同意 Agent | 没有正式状态 | 记录分歧；语气和教学感觉由用户偏好决定 |
| 专家或工具不可用 | 报告限制 | 报告限制；高风险内容没有所需人类审核时不能成为 `verified` |

### 3.5 人类检查点任务包

当前版不再把完整的原始审计报告直接倾倒给人类审核者。每个检查点使用以下格式：

```text
checkpoint: path | sample | release
artifact: 文件路径或短摘录
decision_needed: 用一句话说明需要决定什么
options: 有用时提供默认项和替代项
known_risks: 简短风险列表
questions: 不超过五个聚焦问题
```

这是一项监督改进，而不只是写作功能：审核者能直接看到需要作出的决定，而完整证据仍然单独保留。

## 四、渲染与出版对比

### 4.1 初始渲染系统

初始渲染器提供：

- 本地 KaTeX 数学公式渲染；
- Highlight.js 代码高亮；
- 内联本地 CSS、JavaScript、图片和 SVG；
- 可移植的屏幕和打印 CSS；
- 渐进增强；
- 分离的学生版和答案版 HTML；
- 基于 Playwright 的 PDF 生成；
- 严格结构验证。

视觉系统是单一的学术/技术呈现。它具备可移植性和一致性，但不支持针对不同场景的主题。

### 4.2 当前渲染系统

当前渲染器增加：

- `scholarly`：克制的学术教材呈现；
- `technical`：面向系统和代码的呈现；
- `editorial`：面向案例和叙事的呈现；
- `high-contrast`：面向可访问性和投影场景的呈现；
- 源数据中的 `metadata.theme`；
- 两份 HTML 根节点中的 `data-theme`；
- `manifest.json` 中的主题记录；
- `--theme <name>` 构建覆盖参数；
- 对应的 `--theme <name>` 验证覆盖参数；
- 对不支持的主题进行严格拒绝；
- 每个主题对应的打印样式覆盖。

主题只改变设计变量和封面处理，不改变内容层级或文档语义。不同主题之间不维护多份正文。

### 4.3 仍然存在的渲染限制

当前渲染器功能更强，但仍未提供以下能力：

- 对每个主题和每个视口进行自动截图对比；
- 对生成的所有视觉元素自动测量对比度；
- 对自定义 SVG 进行自动语义检查；
- 浏览器内的人类反馈表单；
- 程序化强制验证 `review.status` 是否与实际人类检查记录一致；
- 自动检测正文节奏或自然度。

这些仍然属于人类或项目层面的检查。skill 将它们记录为责任，而不是假装现有脚本已经能够证明它们。

## 五、上下文与复杂度对比

### 初始上下文行为

初始版的 `SKILL.md` 已经相对紧凑，并通过阶段读取详细参考文档。不过，Sub-agent 编排参考文档较大，可能被整体复制到多个提示词中。审核部分也把五轮审核描述为默认路径，容易让每个任务都装载全部 rubric。

### 当前上下文行为

当前版增加了阶段映射：

| 阶段 | 最小上下文 |
|---|---|
| Intake | intake 参考和 brief 模板 |
| 研究/路径 | intake 和 research 参考 |
| 大纲/章节 | chapter 和 intake 参考 |
| 风格/样例 | human-style 和 visual 参考 |
| Sub-agent 扩展 | orchestration 和 chapter 参考 |
| 审核 | review profile、research 参考和当前 rubric |
| 构建/渲染 | output 和 visual 参考 |

Sub-agent 现在接收紧凑任务包，而不是完整重复的参考文档。这项改变在保留局部决策所需事实的同时，降低了上下文消耗。

## 六、哪些方面变得更严格

当前版在以下方面更严格：

- 完整内容生成必须等待主要用户决策确认；
- 人类语气/样例和发布检查点变成明确步骤；
- 在要求人类审核的情况下，不能只凭 Agent 审核声明 `verified`；
- 高风险内容必须有独立领域或权威人类审核；
- 构建和验证脚本会检查主题名称；
- 主题预览结果必须与 manifest 元数据一致；
- 必须记录编辑分歧，不能悄悄覆盖；
- 答案隔离被视为物理上的输入/输出边界。

## 七、哪些方面变得更灵活

当前版在以下方面更灵活：

- 短的低风险内容可以使用 `light` 档位；
- 可以跳过与内容无关的审核，并记录跳过原因；
- 如果学习问题需要，章节小节可以偏离标准章节路径；
- 人类风格可以是正式、学术、实践型或混合型；
- 编辑意见不需要满足人为设定的数值分数；
- 用户不必在每个小节后被打断；
- 主题可以根据受众和媒介变化，而不需要复制多份正文。

## 八、剩余风险与下一步改进

这份比较也标出了当前版不应隐藏的限制：

1. 渲染器会验证主题元数据，但不会执行像素级视觉回归。未来可以增加 `render-matrix` 脚本，为每个主题捕获桌面、移动端和打印页面。
2. 模板暴露了人类审核字段，但构建工具还不会强制要求 `verified` 状态必须有签名的人类检查记录。未来的发布 manifest 可以要求检查点证据。
3. skill 有正向风格指导，但没有确定性自然度指标。这是合理的：人类风格应由人评审，而不应被压缩成检测器分数。未来的 eval 可以比较人类对具体性、节奏和语气适配度的评分。
4. 外部写作 skill 被用作研究输入，而不是运行时依赖。它们的工具、词表和文体假设不一定适合教材正文。
5. 当前审核档位按风险而不是文档大小决定，但当分类不明显时，作者仍需说明为什么一个主题属于 `light`、`standard` 或 `high-stakes`。

## 最终评估

初始版已经在教材结构、前置知识逻辑、独立练习、答案隔离、Sub-agent 边界和严格出版验证方面表现很强。它的主要弱点是流程失衡：Agent 侧的正确性机制多于对语气、节奏和发布状态的人类控制，同时审核深度过于统一。

当前版成为更好的通用 skill，是因为它明确区分了五组概念：

1. 内容规模与风险等级；
2. 机械正确性与编辑判断；
3. Agent 审核与人类审核；
4. 章节覆盖契约与正文模板；
5. 内容结构与渲染主题。

因此，当前版在减少不必要流程的同时保留了严谨性，并让用户在生产状态中的角色可见。只有在相应技术门禁和所需人类检查点都完成后，才应将产物描述为 `verified`。
